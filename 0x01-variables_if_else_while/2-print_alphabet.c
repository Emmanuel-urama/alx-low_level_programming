#include <stdio.h>
/**
 * main-this program prints the alphabets
 *
 * Return:this returns zero
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
